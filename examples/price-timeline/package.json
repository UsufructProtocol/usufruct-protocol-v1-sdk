{
  "name": "usufruct-price-timeline",
  "private": true,
  "type": "module",
  "description": "Probe — reconstruct an escrow's historical credit/descent curves drift-zero from events, across an ensemble shape change",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@mysten/sui": "^2.17.0",
    "@usufruct-protocol/sdk": "file:../../packages/sdk"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
