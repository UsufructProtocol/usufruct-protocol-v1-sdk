{
  "name": "usufruct-market-replay",
  "private": true,
  "type": "module",
  "description": "Probe — replay a longer multi-phase market (bids, supersedes, handovers, descents, ×2) through escrow.priceTimeline() / creditHistory()",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@mysten/sui": "^2.17.0",
    "@usufruct-protocol/sdk": "file:../../packages/sdk"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
